export default function SectionTitle({ children }) {
  return (
    <div style={{fontSize:14,fontWeight:500,color:'var(--color-text-secondary)',marginBottom:8,marginTop:4}}>
      {children}
    </div>
  );
}
